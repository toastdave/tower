import * as p from '@clack/prompts';

async function handleCrossPlatform(language: string, packageManager: string) {
  p.note('Setting up Cross Platform Application...');
}

export default handleCrossPlatform;
